# AWS integration placeholder

This folder is reserved for future AWS-specific code/configuration helpers, for example:

- CloudWatch log helpers
- X-Ray tracing setup
- Secrets Manager helpers
- Elastic Beanstalk/EKS deployment utilities

No AWS-specific runtime logic is implemented in `feature/book-crud-api` yet.
